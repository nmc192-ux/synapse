# Operator dashboard auth
NEXT_PUBLIC_SYNAPSE_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_SYNAPSE_WS_URL=ws://127.0.0.1:8000/api/ws
NEXT_PUBLIC_SYNAPSE_PROJECT_ID=project_design_partner_a

# Choose one:
NEXT_PUBLIC_SYNAPSE_BEARER_TOKEN=
NEXT_PUBLIC_SYNAPSE_API_KEY=

# Keep tokens project-scoped and operator-only.
# Do not reuse the same token across different partner projects.
